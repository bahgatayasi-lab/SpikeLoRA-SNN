# Legacy implementation

These files are retained only for development history and traceability. They use the earlier experimental workflow, including automatic target detection and a preliminary patch-based Transformer baseline.

They must not be used to reproduce the corrected manuscript results. Use `code/spikelora_ts_r2_colab.py`, the completed R2 notebook, and protocol `SpikeLoRA-TS-R2-corrected-v1` instead.
